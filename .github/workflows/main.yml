name: build-github-actions-workflow
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: demo workflow job
        run: echo This is a demo start workflow
    # Test create artificat
      - name: Create artifact files
        run: |
          mkdir -p task1/build/dir-1
          echo "Hello world from file #2" > path/to/dir-1/file1.txt
      
