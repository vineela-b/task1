name: GitHub Actions zip
on: [push]
jobs:
  To-Zip:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v3
      
      - name: Create zip file
        run: zip -r build.zip .  -x ".git/*" ".github/*"
